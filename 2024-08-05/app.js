// app.js
const express = require('express');

const app = express();
app.use(express.json());

const times = ["palmeiras", "corinthias", "flamengo"];
const time

app.post('/api/new-game', () => {

})